<table class="table table-hover table-condensed">
	<thead>
  <tr>
    <th>From</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  </thead>
  <tbody>
	<% friends.each do |friend| %>
		<tr>
	    <td><%= link_to truncate("#{friend.name}", length: 40), user_path(friend) %></td>
	    <td><%= link_to 'Remove', friend_deny_path(friend), remote: true %></td>
	    <td><%= link_to 'Block', friend_block_path(friend), remote: true %></td>
	  </tr>
	<% end %>
	</tbody>
</table>