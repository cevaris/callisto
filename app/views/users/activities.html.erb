<div class="row">

	<div class="span3" >
		<%= render partial: 'user', locals: { user: @current_user } %>
	</div><!-- user -->

	<div class="span9">
		
		<% if !@activities.empty? %>
		<h3> <%= params[:action].titlecase %> Activities</h3>
		<%= render :partial => 'table_activities', locals: { activities: @activities } %>
		<% else %>
		<h3>No Activities Found</h3>
		<% end %>

	</div>

</div>