<header id="header" class="navbar navbar-static-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
    	<% if signed_in? %>
      <%= link_to t('global.app_name'), root_path, class: "brand" %>
      <% else %>
      <%= link_to t('global.app_name'), root_path, class: "brand" %>
      <% end %>
      <nav>
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <div class="nav-collapse collapse">
          <ul class="nav pull-right">
          		<li><%= link_to 'Home', root_path %></li>
            	<li><%= link_to 'Search', activities_path %></li>
            <% if signed_in? %>
            	<li><%= link_to "#{current_user.name.titlecase}", current_user %></li>
              <li><%= link_to "Friends", friends_url %></li>
            	<li><%= link_to t('signin.settings'), settings_path %></li>
              <li> <%= link_to t('signin.signout'), signout_path, method: "delete" %> </li>              
            <% else %>
              <li><%= link_to t('signup.link'), signup_path %></li>
              <li><%= link_to t('signin.link'), signin_path %></li>
            <% end %>
          </ul>
        </div>
      </nav>
    </div>
  </div>
</header>
